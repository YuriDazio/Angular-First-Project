import { MarcatextoDirective } from './marcatexto.directive';

describe('MarcatextoDirective', () => {
  it('should create an instance', () => {
    const directive = new MarcatextoDirective();
    expect(directive).toBeTruthy();
  });
});
